# ── Backend API URLs (see src/config.js) ──
# Copy this file to `.env` and fill in your database-backed backend URLs.
# Leave empty to run the app without backend calls.

# Main backend: tasks, team, clients, payments, activities, chat, auth
VITE_API_BASE_URL=

# Daily task sheet backend: punch in/out + per-task timer rows
VITE_DAILY_SHEET_WEB_APP_URL=

# Voice AI assistant (optional)
VITE_ELEVENLABS_AGENT_ID=
